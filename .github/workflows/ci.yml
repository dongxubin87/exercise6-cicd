name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Simulate Docker Build
      run: |
        echo "Docker image build simulated"

    - name: Simulate Push to Registry
      run: |
        echo "Image push simulated"

    - name: Pipeline Success Marker
      run: echo "CI pipeline finished successfully"
